// ---------------------------------------- https://nodejs.org/dist/latest-v14.x/docs/api/ --------------------------------------------

// The REPL feature of node is very useful in experimenting with Node.js code and to debug JavaScript codes.
// R(Read) -> Reads user input parses the input into javascript data-structure , and stores in memory.
// E(Eval) -> Takes and evalutes the data-structures.
// P(Print) -> Prints the result.
// L(Loop) -> Loops the above command until the user presses ctrl-C twice.

// node.js is just like running javascript on server
// node.js environment is like chrome console in our terminal

// write node in bash terminal or any terminal to open its repl environment
// We can do below things in node environment with REPL:-
// 1) JS Expression eg:- (+, -, /, %).
// 2) use Variables.
// 3) Multiline code / it is useful to write Loops
    // eg:-
    // for(var i=0;i<=5;i++){
    //     ... console.log(i);};
// 4) Use (_) underscore to get last results.
// 5) We can use editor mode. write .editor to enter in this mode.

// Press Tab twice in node environment to see node.js properties and method 
//  to get info about any methods write the method name and press enter in node environment